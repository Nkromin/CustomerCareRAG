[project]
name = "pythonproject"
version = "0.1.0"
description = "LangChain application with Groq API"
requires-python = ">=3.12"
dependencies = [
    "langchain",
    "langchain-groq",
    "python-dotenv"
]
